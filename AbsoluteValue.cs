using System;

namespace AlgebraC__
{
    static class AbsoluteValue
    {
        public static double AbsoluteValueOfRealNumber(double baseValue)
        {
            double absoluteValue = Math.Abs(baseValue);

            return absoluteValue;
        }
    }
}